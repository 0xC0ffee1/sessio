# Sessio Coordinator Configuration
# This file contains all configurable settings for the coordinator deployment

# SECURITY: Change these values in production!
POSTGRES_PASSWORD=sessio123
DATABASE_URL=postgres://sessio:sessio123@postgres:5432/sessio_coordinator

# Session Security - MUST be changed in production
COOKIE_SECRET_KEY=LZJ84ygvWzPNZspElrWo0tmDDATAAebMiPlB/iFosUc=

# The publicly available coordinator url
COORDINATOR_URL=http://localhost:8000/api/

# The url that users will use to access the web ui
WEB_UI_URL=http://localhost:8000

# Frontend uses this to connect to the coordinator API.
# Use "http://coordinator:2223" to connect to the container when using docker compose.
API_BASE_URL=http://coordinator:2223
###

# Should the coordinator use HTTP or HTTPS?
# WARNING: Only use HTTP for development - HTTPS required for production
USE_HTTP=true

# Rate Limiting Configuration
MAX_REQUESTS_PER_MINUTE=60
MAX_PAYLOAD_SIZE=1048576
